namespace Reactor {
    public class CoolingRoom : Room {
        CoolingRoom (string img,string desc) {
            image = img; //this works right? OOP moment
            description = desc;
        }
    }
}
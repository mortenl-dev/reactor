namespace Reactor {
    public class FuelRoom : Room {
        FuelRoom(string img, string desc) {
            image = img; //this works right? OOP moment
            description = desc;
        }
    }
}